#! /usr/bin/env Python3

#read in file
InFileName = "Bloom_etal_2018_Reduced_Dataset"

#line count

LineNumb = 0

#make loop to read through each line
for line in InFileName:
	if LineNumb >= 0:
		print "taxon name, dia size"
	TotalSize = sum log of body sizes for each taxon
	Print TotalSize

InFile.close()
